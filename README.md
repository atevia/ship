# Atevia Ship 🚀

Internal CLI tool to securely transfer folders between servers over SSH.

## Install
```bash
curl -fsSL https://raw.githubusercontent.com/atevia/ship/main/scripts/install.sh | bash
